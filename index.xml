<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Reproducible research course</title>
<link>https://vucka7.github.io/blog/</link>
<atom:link href="https://vucka7.github.io/blog/index.xml" rel="self" type="application/rss+xml"/>
<description>A blog built with Quarto</description>
<generator>quarto-1.10.18</generator>
<lastBuildDate>Mon, 14 Sep 2026 00:00:00 GMT</lastBuildDate>
<item>
  <title>Day 2 of the 5-day Odissey</title>
  <dc:creator>Eva Rogelj</dc:creator>
  <link>https://vucka7.github.io/blog/posts/day_2/</link>
  <description><![CDATA[ 






<p>Day 2 Lecture 1: setting up an environment. Pixi [Quarto] (https://pixi.prefix.dev/latest/workspace/environment/)</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1">wget <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>qO<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span> https<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">//</span>pixi.sh<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>install.sh <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">|</span> sh</span></code></pre></div></div>
</div>
<p>Everytime you need to initiate a pixi environment in the same folder, you’re working in</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb2-1">pixi init name_pixi_training <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>c conda<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>forge <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>c bioconda</span></code></pre></div></div>
</div>
<p>There will be a pixi.toml file. It would look a little like this</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb3-1"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>[workspace]</span>
<span id="cb3-2"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">2</span>channels <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span> [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"conda-forge"</span>, <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"bioconda"</span>]</span>
<span id="cb3-3"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">3</span>name <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"amrei_pixi_training"</span></span>
<span id="cb3-4"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">4</span>platforms <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span> [<span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"linux-64"</span>]</span>
<span id="cb3-5">version <span class="ot" style="color: #003B4F;
background-color: null;
font-style: inherit;">=</span> <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"0.1.0"</span></span>
<span id="cb3-6"></span>
<span id="cb3-7"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">5</span>[tasks]</span>
<span id="cb3-8"></span>
<span id="cb3-9"><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">6</span>[dependencies]</span></code></pre></div></div>
</div>
<p>1 Overview section of the environment. 2 Added channels - these are the sources we allow for our dependencies/ tools. 3 The name of the environment. 4 Operating system the environment is optimised for. 5 This is where we can define tasks - interesting, but not covered in this course. 6 Here will be the list of tool installed within the environment (once we have added them).</p>
<p>pixi.lock The .lock file give you information on the channels you have decided to add, as well as the information on where the packages were downloaded from, license information, md5 information, and more.</p>
<p>IMPORTANT: Do not delete the .toml or .lock files, or you will break your environment!</p>
<p>If you want to add a dependency, you use add</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb4-1">pixi add quarto</span></code></pre></div></div>
</div>
<p>You could enter an environment with</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb5-1">pixi shell</span></code></pre></div></div>
</div>
<p>You exit the environment with exit</p>



 ]]></description>
  <category>news</category>
  <category>code</category>
  <category>analysis</category>
  <guid>https://vucka7.github.io/blog/posts/day_2/</guid>
  <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
  <media:content url="https://vucka7.github.io/blog/posts/day_2/odissey_day2.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Day 1 of the 5-day Odissey</title>
  <dc:creator>Eva Rogelj</dc:creator>
  <link>https://vucka7.github.io/blog/posts/day_1/</link>
  <description><![CDATA[ 






<p>Day 1 - only beginning to dream about being a bioinformatician</p>
<p>Data Management and reproducible research <img src="https://vucka7.github.io/blog/posts/day_1/data_life_cycle.png" class="img-fluid" alt="Alt text"></p>
<p>FAIR principles</p>
<p>FAIR principles rely on good data management practices in all phases of research:</p>
<p>Research documentation Data organisation Information security Ethics and legislation</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://vucka7.github.io/blog/posts/day_1/fair-data-principles.webp" class="img-fluid figure-img"></p>
<figcaption>Alt text</figcaption>
</figure>
</div>
<p>Best practices for file organisation</p>
<ul>
<li>There is a folder for the raw data, which does not get altered.</li>
<li>Code is kept separate from data.</li>
<li>Use a version control system (at least for code) – e.g.&nbsp;git.</li>
<li>There should be a README in every directory, describing the purpose of the directory and its contents.</li>
<li>Use file naming schemes that makes it easy to find files and understand what they are (for humans and machines) and document them.</li>
<li>Use non-proprietary formats – .csv rather than .xlsx</li>
</ul>
<p>Literate programming - Document your methods and workflows. - Document where and when you downloaded data. - Document the versions of the software that you ran.</p>
<p>Future learnings in the course: - Version control - environment managers - containers in bioinformatics - workflow manager</p>
<p>Github: Useful commands:</p>
<div class="cell">
<div class="code-copy-outer-scaffold"><div class="sourceCode cell-code" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode r code-with-copy"><code class="sourceCode r"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># CLONE A REPO</span></span>
<span id="cb1-2">git clone https<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">:</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">//</span>github.com<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>username<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">/</span>repo.git</span>
<span id="cb1-3"></span>
<span id="cb1-4"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># BASIC WORKFLOW</span></span>
<span id="cb1-5">git status                    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># See what changed</span></span>
<span id="cb1-6">git add .                     <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Stage all changes</span></span>
<span id="cb1-7">git commit <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>m <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"message"</span>       <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Commit with message</span></span>
<span id="cb1-8">git push                      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Push to GitHub</span></span>
<span id="cb1-9">git pull                      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Pull latest changes</span></span>
<span id="cb1-10"></span>
<span id="cb1-11"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># BRANCHES</span></span>
<span id="cb1-12">git branch                    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># List branches</span></span>
<span id="cb1-13">git branch new<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>branch         <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Create new branch</span></span>
<span id="cb1-14">git checkout new<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>branch       <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Switch to branch</span></span>
<span id="cb1-15">git checkout <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>b new<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>branch    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Create and switch</span></span>
<span id="cb1-16">git push origin new<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>branch    <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Push branch to GitHub</span></span>
<span id="cb1-17"></span>
<span id="cb1-18"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># UNDO CHANGES</span></span>
<span id="cb1-19">git restore filename          <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Undo changes to file</span></span>
<span id="cb1-20">git reset HEAD<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">~</span><span class="dv" style="color: #AD0000;
background-color: null;
font-style: inherit;">1</span>              <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Undo last commit (keep changes)</span></span>
<span id="cb1-21">git revert HEAD               <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Undo last commit (create new commit)</span></span>
<span id="cb1-22"></span>
<span id="cb1-23"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># VIEW HISTORY</span></span>
<span id="cb1-24">git log                       <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># See commits</span></span>
<span id="cb1-25">git log <span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">--</span>oneline             <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Shorter view</span></span>
<span id="cb1-26">git diff                      <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># See what changed</span></span>
<span id="cb1-27"></span>
<span id="cb1-28"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># MERGE BRANCHES</span></span>
<span id="cb1-29">git checkout main             <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Switch to main</span></span>
<span id="cb1-30">git merge new<span class="sc" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">-</span>branch          <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Merge branch into main</span></span>
<span id="cb1-31"></span>
<span id="cb1-32"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># STASH (TEMPORARY SAVE)</span></span>
<span id="cb1-33">git stash                     <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Save changes temporarily</span></span>
<span id="cb1-34">git stash pop                 <span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;"># Restore changes</span></span></code></pre></div></div>
</div>



 ]]></description>
  <category>news</category>
  <category>code</category>
  <category>analysis</category>
  <guid>https://vucka7.github.io/blog/posts/day_1/</guid>
  <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
  <media:content url="https://vucka7.github.io/blog/posts/day_1/suitcase.jpeg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Day 3 of the 5-day Odissey</title>
  <dc:creator>Eva Rogelj</dc:creator>
  <link>https://vucka7.github.io/blog/posts/day_3/</link>
  <description><![CDATA[ 






<p>Day 3 - Introduction to Nextflow When developing your code in bioinformatics, you will likely use different tool for different parts of your analyses. Traditionally, you would have about one script per tool, all of which you deploy by hand, one after the other. Together, this is called a workflowor pipeline.</p>
<p>Manual deployment of pipelines can be tedious, especially if you have analyses with many steps, or many samples of different sizes that might need a varying amount of computational power. Luckily for you, other bioinformaticians and software developers have developed something to make your life much easier:</p>
<p>Workflow managers Workflow managers provide a framework for the creation, execution, and monitoring of pipeline. &lt;…&gt; They simplify pipeline development, optimize resource usage, handle software installation and versions, and run on different compute platforms, enabling workflow portability and sharing. Wratten et al.&nbsp;(2021) Nature Methods</p>
<p>With workflow managers you can develop an automated pipeline from your scripts that can then be run on a variety of systems. Once it is developed, execute a single command to start the pipeline. The manager then coordinates the deployment of the scripts in the appropriate sequence, monitors the jobs, handles the file transfers between scripts, gathers the output, and handles re-execution of failed jobs for you. Workflow managed pipelines can run containers, which eliminates software installation and version conflicts.</p>
<p>That means that by design the pipelines are:</p>
<p>portable more time efficient (no more downtime between pipeline steps) more resource efficient (mostly, but this might vary depending how skilled a developer you yourself are) easier to install (especially when combined with containers, or environment managers) more reproducible There are in principle two different flavors of workflow managers: snakemake, and nextflow. In this course we will be introducing you to nexflow.</p>
<div class="quarto-figure quarto-figure-center">
<figure class="figure">
<p><img src="https://vucka7.github.io/blog/posts/day_3/nextflow.jpg" class="img-fluid figure-img"></p>
<figcaption>Alt text</figcaption>
</figure>
</div>
<p>In nextflow, your scripts are turned into processes, connected by channels that contain the data - input, output etc. The order of the processes, and their interaction with each other, is specfied in the workflow scope.</p>
<p>The executable part of the processes, the so called script, can be written in any language, so in theory you could always choose the language that is best suited for the job (in practice you might be limited to the languages you know). However, the modularity of the processes allows for easy re-use of existing scripts and processes.</p>
<p>When moving the pipeline from one system to another, the script stays the same and does not change, the same containers are used. The only thing that changes are the parameters that pertain to the environment and available resources. In other words, with nextflow, the functional logic, the processes, are separated from the executive (how the workflow runs). This makes the nextflow pipelines highly interoperable and portable. They can be run on various platforms, such as HPC clusters, local computers, cloud systems etc..</p>
<p>The pipelines can be integrated with version control tools, such as git or bitbucket, and containers technologies, such as apptainer or docker. This makes the pipeline very reproducible.</p>
<p>The nextflow pipelines are extremely scalable, can be developed on a few samples and easily be run on hundreds or thousands of samples. When possible, processes are run in parallel automatically.</p>
<p>Nexflow performs automatic checks on the processes and their in- and output. It can automatically resume execution at a point of failure without having to re-compute successfully completed parts.</p>
<p>Nextflow is open source.</p>
<p>Here is a more visual summary of some of the points above: Write code in any language orchestrate tasks with dataflow programming define software dependencies via containers built-in version control with Git Task orchestration and execution</p>
<p>How to work with Nextflow? Any process can define one or more channels as an input and output. The interaction between these processes, and ultimately the workflow execution flow itself, is implicitly defined by these input and output declarations.</p>
<p>Process block looks a little bit like this:</p>
<pre class="{bash}"><code>#| eval: false
process PROCESS_NAME{
    // directives
    container
    
    input:
      data z
      data y
      data x

    script:
      task1
      task2
      task3

    output:
      output x
      output y
      output z
}</code></pre>
<p>We started with:</p>
<pre class="{bash}"><code>#| eval: false
pixi init -c conda-forge -c bioconda</code></pre>
<p>Something like this is a code in a .txt file</p>
<pre class="{bash}"><code>#| eval: false
#!/usr/bin/env nextflow

params.greeting = 'Hello world!'

process SPLITLETTERS {
    input:
    val x

    output:
    path 'chunk_*'

    script:
    """
    printf '$x' | split -b 6 - chunk_
    """
}

process CONVERTTOUPPER {
    input:
    path y

    output:
    stdout

    script:
    """
    cat $y | tr '[a-z]' '[A-Z]'
    """
}

workflow {
    greeting_ch = Channel.of(params.greeting)
    letters_ch = SPLITLETTERS(greeting_ch)
    results_ch = CONVERTTOUPPER(letters_ch.flatten())
    results_ch.view{ it }
}</code></pre>
<p>If you run:</p>
<pre class="{bash}"><code>#| eval: false
pixi run nextflow run hello.nf</code></pre>
<p>Output looks like N E X T F L O W ~ version 25.04.7</p>
<p>Launching <code>hello.nf</code> [jolly_faraday] DSL2 - revision: f5e335f983</p>
<p>executor &gt; local (3) [96/fd5f07] SPLITLETTERS (1) [100%] 1 of 1 ✔ [7e/dad424] CONVERTTOUPPER (2) [100%] 2 of 2 ✔ HELLO WORLD!</p>
<p>The order might be different, both processes are executed next to each other.</p>
<p>If you want to change something, but not re-run the whole thing, you can use:</p>
<pre class="{bash}"><code>#| eval: false
pixi run nextflow run hello.nf -resume</code></pre>
<p>Workflow parameters can be altered As we have seen workflow parameters are simply declared by prepending the prefix params to a variable name, separated by a dot character (e.g.&nbsp;params.greeting). Their value can alternatively be specified on the command line by prefixing the parameter name with a double dash character, i.e.&nbsp;–paramName.</p>
<p>Nextflow log can get crowded - use this command to remove log files.</p>
<pre class="{bash}"><code>#| eval: false
pixi run nextflow log
pixi run nextflow clean -f
pixi run nextflow clean -before &lt;RUN NAME&gt; -f</code></pre>
<p>Nextflow RNAseq pipeline</p>
<p>Salmon — quantifies transcripts from RNA-seq data. FastQC — performs quality control on high-throughput sequencing data. MultiQC — searches a directory for analysis logs and compiles them into a single HTML report.</p>
<p>Step by step — and why each tool is used when:</p>
<p>Index the transcriptome reference — salmon index. Salmon cannot quantify reads against a plain FASTA; it first needs to turn the reference into a fast, searchable index. We do this once at the start, and every sample reuses the same index.</p>
<p>Quantify expression — salmon quant. For each sample, Salmon maps the read pair against the index and estimates how much of each transcript is present. Here Salmon turns raw reads into numbers to analyse.</p>
<p>Check read quality — FastQC. In parallel, FastQC inspects the raw reads of each sample (base quality, adapter content, and so on). We run it alongside quantification, not after, because quality control is independent of the counting.</p>
<p>Summarise everything — MultiQC. Finally, MultiQC gathers the logs from all the samples (Salmon and FastQC) into one HTML report, so you read a single summary instead of opening dozens of files. It waits for the all earlier steps to finish before it runs.</p>
<p>First step: Set the executor You can specify your computer power in a file called nextflow.config.</p>
<pre class="{bash}"><code>#| eval: false
process{
    executor = "slurm"
    time = '2.h'  // Set default time limit for all processes
    
    // Other settings
    cpus = 4
}

resume = true
singularity.enabled = true

executor {
    account = 'your_project_account'
}</code></pre>
<p>This sets the executor for every process to slurm.</p>
<p>We set resume to true, so it is automatically used for all our runs (which means we do not have to specify this in our nextflow run command anymore).</p>
<p>We enable singularity and set some singularity run options.</p>
<p>Lastly, we specify the account name for the slurm execution, replace the placeholder with the actual compute allocation ID.</p>
<p>This nextflow.config file is implicitly called when we execute nextflow from the folder it is in. We do not have to explicitely name it in the nextflow run command.</p>
<p>Define the workflow parameters Parameters are inputs and options that can be changed when the workflow is run.</p>
<p>The script script1.nf defines the workflow input parameters:</p>
<p>#!/usr/bin/env nextflow</p>
<p>params.reads = “<img src="https://latex.codecogs.com/png.latex?projectDir/data/ggal/gut_%7B1,2%7D.fq%22%0Aparams.transcriptome_file%20=%20%22">projectDir/data/ggal/transcriptome.fa” params.multiqc = “$projectDir/multiqc”</p>
<p>workflow { println “reads: $params.reads” }</p>
<p>Run it by using the following command:</p>
<p>pixi run nextflow run script1.nf</p>
<p>Note Note that we do not need to specify that we want to use the nextflow.config file. Since it is in the same directory it will be used automatically.</p>
<p>ImportantTo do for you Modify script1.nf by adding a fourth parameter named outdir and set it to a default path that will be used as the workflow output directory.</p>
<p>Create a transcriptome index file Nextflow allows the execution of any command or script by using a process definition.</p>
<p>A process is defined by providing three main declarations: the process input, output, and the command script.</p>
<p>To add a transcriptome INDEX processing step, add the following code blocks to your script1.nf. Alternatively, these code blocks have already been added to script2.nf.</p>
<p>process INDEX { input: path transcriptome</p>
<pre><code>output:
path 'salmon_index'

script:
"""
salmon index --threads $task.cpus -t $transcriptome -i salmon_index
"""</code></pre>
<p>}</p>
<p>Additionally, replace the existing workflow scope with an input channel definition and the index process:</p>
<p>workflow { index_ch = INDEX(file(params.transcriptome_file, checkIfExists: true)) }</p>
<p>Here, the params.transcriptome_file parameter is used as the input for the INDEX process. The INDEX process (using the salmon tool) creates salmon_index, an indexed transcriptome that is passed as an output to the index_ch channel.</p>
<p>Run with:</p>
<p>pixi run nextflow run script1.nf</p>
<p>Caution Why doesn’t this work? What have we forgotten?</p>
<p>EITHER add Salmon to your environment pixi add salmon</p>
<p>OR add Salmon in a container to the process Adding a container to the process and running the tool via the container is more reproducible than adding it to your environment. Use the container directive and add the URL to the container to the process scope, above the input block:</p>
<p>container ‘oras://community.wave.seqera.io/library/salmon:2.7.0–cb05356ce9eb88b7’</p>
<p>Note You can also specify a process specific output directory in the process block:</p>
<p>publishDir “$params.outdir/salmon”</p>
<p>This command line would typically be located just below the container directive.</p>
<p>By default publishDir links the published files back into the work/ directory rather than copying them. This is fast and saves space, but it means the files disappear if you clean work/. Add mode: ‘copy’ when you want an independent copy that survives a cleanup, for example a result you intend to keep or share.</p>
<p>Define run time environment Remember how Nextflow is big on execution abstraction? This is where we will put this concept into practice. In the nextflow.config add the following just before the closing squiggly bracket from the process definitions:</p>
<pre><code>withName:'INDEX'{
    time = 15.m
    cpus = 2
}</code></pre>
<p>With this we specify the alloted time and cpus for this specific process.</p>
<p>To specify memory add mem = xx.GB - check with your cluster to see how much memory is available and how to specify it. In our case we don’t need to because the memory implicit in the 2 cpus is enough for our purposes.</p>
<p>pixi run nextflow run script1.nf</p>
<p>Interlude: Visualize tuples Salmon needs both reads of a pair together to quantify a sample. Our data folder holds six files — three samples, two files each:</p>
<p>gut_1.fq gut_2.fq liver_1.fq liver_2.fq lung_1.fq lung_2.fq If we simply collected every .fq into one channel, Nextflow would see six loose files with nothing saying that gut_1 and gut_2 belong together. We need them grouped by sample.</p>
<p>That is what the fromFilePairs channel factory does, and we can have a look at it in script3.nf. The {1,2} in the pattern marks the part that differs between the two files of a pair; Nextflow groups the files that share everything else:</p>
<p>read_pairs_ch = channel.fromFilePairs(params.reads)</p>
<p>To see what it produces, view the channel from inside the workflow:</p>
<p>workflow { read_pairs_ch = channel.fromFilePairs(params.reads) read_pairs_ch.view() }</p>
<p>Run it:</p>
<p>pixi run nextflow run script3.nf</p>
<p>It prints one item per sample:</p>
<p>[gut, [/…/data/ggal/gut_1.fq, /…/data/ggal/gut_2.fq]]</p>
<p>Read that line piece by piece — each item is a tuple with two parts:</p>
<p>gut — the sample ID, taken from the shared prefix of the file names, and [gut_1.fq, gut_2.fq] — a list of the two files that belong to that sample. Tip Think of it like matching socks: fromFilePairs puts _1 and _2 together by their shared name, and the {1,2} glob tells Nextflow which part is allowed to differ.</p>
<p>Note params.reads points at gut only, so you see one pair. Point it at all three samples to watch them grouped side by side:</p>
<p>pixi run nextflow run script3.nf –reads ’data/ggal/*_{1,2}.fq’</p>
<p>Because each item carries the ID and both files together, Salmon can process each sample in a single task — and Nextflow runs all three in parallel. That is the behaviour you will use in the next step.</p>
<p>Expression quantification Look at the file “script4.nf”. This script adds a gene expression QUANTIFICATION process and a call to it within the workflow scope. Quantification requires the index transcriptome and RNA-Seq read pair fastq files.</p>
<p>In the workflow scope, note how the index_ch channel is assigned as output in the INDEX process.</p>
<p>Next, note that the first input channel for the QUANTIFICATION process is the previously declared index_ch, which contains the path to the salmon_index.</p>
<p>Also, note that the second input channel for the QUANTIFICATION process, is the read_pair_ch we just created. This being a tuple** composed of two elements (a value: “sample_id” and a list of paths to the fastq reads: “reads”) in order to match the structure of the items emitted by the fromFilePairs channel factory.</p>
<p>**tuple: a data structure consisting of multiple parts.</p>
<p>pixi run nextflow run script4.nf</p>
<p>Execute the same script again with more read files, as shown below:</p>
<p>pixi run nextflow run script4.nf –reads ’data/ggal/*_{1,2}.fq’</p>
<p>You will notice that the QUANTIFICATION process is executed multiple times.</p>
<p>Nextflow parallelizes the execution of your workflow simply by providing multiple sets of input data to your script.</p>
<p>ImportantTo do for you Add the process specific runtime environment definition for QUANTIFICATION to nextflow.config.</p>
<p>Quality control Now we want to add another process, using our beloved FastQC to check the samples. The input is the same as the read pairs used in the QUANTIFICATION step.</p>
<p>ImportantTo do for you Sit together with your neighbour and first think 5 minutes what you need to do to implement this new process.</p>
<p>We will then discuss in class.</p>
<p>MultiQC This last step collects the outputs from the QUANTIFICATION and FASTQC processes to create a final report using MultiQC.</p>
<p>Again, what do you need to consider when adding this process to the pipeline?</p>
<p>Tip We only want one task of MultiQC to be executed to produce one report. For this, it needs to wait for the report generating processes to stop. We can use the mix channel operator to combine the two channels followed by the collect operator, to return the complete channel contents as a single element.</p>
<p>So the new line in the workflow scope looks like this:</p>
<p>MULTIQC(quant_ch.mix(fastqc_ch).collect())</p>



 ]]></description>
  <category>news</category>
  <category>code</category>
  <category>analysis</category>
  <guid>https://vucka7.github.io/blog/posts/day_3/</guid>
  <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
  <media:content url="https://vucka7.github.io/blog/posts/day_3/odissey_day2.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Day 4 of the 5-day Odissey</title>
  <dc:creator>Eva Rogelj</dc:creator>
  <link>https://vucka7.github.io/blog/posts/day_4/</link>
  <description><![CDATA[ 






<section id="day-4-pipelines---the-horror-nf-core" class="level1">
<h1>Day 4: Pipelines - the horror: nf-core</h1>
<section id="nfcore-introduction" class="level2">
<h2 class="anchored" data-anchor-id="nfcore-introduction">nfcore introduction</h2>
<p><a href="https://nf-co.re/">Nf-core link</a></p>
<p>Nf-core is a very active community around nextflow. Volunteers develop nextflow pipelines around a variety of bioinformatic data.</p>
<p>All nf-core pipelines are open source and the source code is available on github. The pipelines are developed by volunteers, who can have a very varied background.</p>
<p>A standard one that we tried first is rnaseq: <a href="https://nf-co.re/rnaseq/3.26.0/">rnaseq</a></p>
<p>But one applicable to my research if airrflow: <a href="https://nf-co.re/airrflow/5.1.1/">airrflow</a></p>
<p>Using an nf-core pipeline: Set pixi environment</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb1" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb1-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb1-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pixi</span> init nextflow_test <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-c</span> conda-forge <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-c</span> bioconda</span></code></pre></div></div>
<p>Add nf-core and nextflow</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb2" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb2-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb2-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pixi</span> add nextflow nf-core</span></code></pre></div></div>
<p>Can check if it worked by:</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb3" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb3-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb3-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pixi</span> run nextflow <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-version</span></span>
<span id="cb3-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pixi</span> run nf-core <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--help</span></span>
<span id="cb3-4"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">pixi</span> run nextflow run hello</span></code></pre></div></div>
<p>You need a configuration file</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb4" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb4-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb4-2"><span class="fu" style="color: #4758AB;
background-color: null;
font-style: inherit;">wget</span> https://raw.githubusercontent.com/hpc2n/intro-course/master/exercises/NEXTFLOW/INTERACTIVE/hpc2n.config</span></code></pre></div></div>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb5" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb5-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb5-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">//</span> Config profile for HPC2N</span>
<span id="cb5-3"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">params</span> {</span>
<span id="cb5-4">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">config_profile_description</span> = <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'Cluster profile for HPC2N'</span></span>
<span id="cb5-5">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">config_profile_contact</span> = <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'Pedro Ojeda @pojeda'</span></span>
<span id="cb5-6">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">config_profile_url</span> = <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'https://www.hpc2n.umu.se/'</span></span>
<span id="cb5-7">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">project</span> = null</span>
<span id="cb5-8">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">clusterOptions</span> = null</span>
<span id="cb5-9">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">max_memory</span> = 128.GB</span>
<span id="cb5-10">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">max_cpus</span> = 28</span>
<span id="cb5-11">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">max_time</span> = 168.h</span>
<span id="cb5-12">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">email</span> = <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'pedroojeda2011@gmail.com'</span></span>
<span id="cb5-13"><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">}</span></span>
<span id="cb5-14"></span>
<span id="cb5-15"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">singularity</span> {</span>
<span id="cb5-16">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">enabled</span> = true</span>
<span id="cb5-17"><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">}</span></span>
<span id="cb5-18"></span>
<span id="cb5-19"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">process</span> {</span>
<span id="cb5-20">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">executor</span> = <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">'slurm'</span></span>
<span id="cb5-21">  <span class="ex" style="color: null;
background-color: null;
font-style: inherit;">clusterOptions</span> = { <span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"-A </span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">$params</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">.project </span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">${params</span><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">.clusterOptions ?: ''</span><span class="va" style="color: #111111;
background-color: null;
font-style: inherit;">}</span><span class="st" style="color: #20794D;
background-color: null;
font-style: inherit;">"</span> }</span>
<span id="cb5-22"><span class="er" style="color: #AD0000;
background-color: null;
font-style: inherit;">}</span></span></code></pre></div></div>
<p>I ran the test data with airrflow</p>
<div class="code-copy-outer-scaffold"><div class="sourceCode" id="cb6" style="background: #f1f3f5;"><pre class="sourceCode bash code-with-copy"><code class="sourceCode bash"><span id="cb6-1"><span class="co" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">#| eval: false</span></span>
<span id="cb6-2"><span class="ex" style="color: null;
background-color: null;
font-style: inherit;">nextflow</span> run nf-core/airrflow <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-r</span> 5.1.1 <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">-profile</span> test <span class="at" style="color: #657422;
background-color: null;
font-style: inherit;">--outdir</span> <span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&lt;</span>OUTDIR<span class="op" style="color: #5E5E5E;
background-color: null;
font-style: inherit;">&gt;</span></span></code></pre></div></div>
<iframe src="Airrflow results summary report.pdf" width="100%" height="800px"></iframe>


</section>
</section>

 ]]></description>
  <category>news</category>
  <category>code</category>
  <category>analysis</category>
  <guid>https://vucka7.github.io/blog/posts/day_4/</guid>
  <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
  <media:content url="https://vucka7.github.io/blog/posts/day_4/odissey_day2.jpg" medium="image" type="image/jpeg"/>
</item>
<item>
  <title>Day 2 of the short Odissey</title>
  <dc:creator>Eva Rogelj</dc:creator>
  <link>https://vucka7.github.io/blog/posts/day_5/</link>
  <description><![CDATA[ 






<p>Day 2 Lecture 1: setting up an environment. Pixi [Quarto] (https://pixi.prefix.dev/latest/workspace/environment/)</p>



 ]]></description>
  <category>news</category>
  <category>code</category>
  <category>analysis</category>
  <guid>https://vucka7.github.io/blog/posts/day_5/</guid>
  <pubDate>Mon, 14 Sep 2026 00:00:00 GMT</pubDate>
  <media:content url="https://vucka7.github.io/blog/posts/day_5/odissey_day2.jpg" medium="image" type="image/jpeg"/>
</item>
</channel>
</rss>
